name: Deploy to Dreamhost
run-name: ${{ github.actor }} is pushing to Dreamhost!
on: [push]
jobs:
  Push-To-Dreamhost:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'
      - run: npm ci
      - run: npm run build --if-present
      # - run: echo " Job was triggered by a ${{ github.event_name }} event."
      # - name: Install SSH Key
      #   uses: shimtaro/ssh-key-action@v2
      #   with:
      #     key: ${{ secrets.DREAMHOST_SECRET }}
      #     known_hosts: 'derp'
      # - name: Adding Known Hosts
      #   run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
      - name: Build project
        run: npm run build
      
      # - name: Deploy into Dreamhost
      #   run: rsync -avz ./dist/ ${{ secrets.SSH_USER
